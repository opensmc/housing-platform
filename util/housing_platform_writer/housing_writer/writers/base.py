class TableWriter():
    """
    Base class for classes that write out tables.

    Put here as a placeholder for when I need it.
    """

    def __init__(self, worksheet, current_row, current_col):
        self.worksheet = worksheet
        self.current_row = current_row
        self.current_col = current_col
